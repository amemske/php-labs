<?php

//A public abstract method is a method that is declared in an abstract class or interface, but does not provide an implementation for the method.

abstract class Animal {
    public abstract function makeSound(): string;
}
